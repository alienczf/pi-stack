---
description: Review and install a pstack upstream update without updating pi-stack
---
Read the `update-pstack` skill and execute it. Keep pi-stack on its current Git revision. Stop before apply if the exact upstream diff breaks the Pi adapter.
